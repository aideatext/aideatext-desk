{
  "name": "desk-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.4.0",
    "vitest": "^5.0.0",
    "pdf-lib": "^1.17.1"
  },
  "dependencies": {
    "pdfjs-dist": "^4.9.155"
  }
}
